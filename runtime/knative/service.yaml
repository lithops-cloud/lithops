apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: pywren-action
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: docker.io/sadek/kpywren
          containerConcurrency: 1
